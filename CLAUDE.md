# Tideline

Read tideline-build-prompt.md before any work. Follow its phases in order.
Stop at every checkpoint and wait for approval.

## Hard rules
- Never call Math.random(). All randomness from the seeded mulberry32 instance.
- Never scroll-jack or disable native scrolling.
- Never obscure résumé content with an effect.
- 60fps desktop / 30fps mobile floor. Cut features, not the budget.
- Do not add anything not in the spec. Propose it at a checkpoint.